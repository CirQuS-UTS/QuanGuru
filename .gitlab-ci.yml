image: "python:3.7"

before_script:
    - python --version
    - pip install -r requirements.txt

stages:
    - Static Analysis
    - Test

mypy:
    stage: Static Analysis
    tags:
        - qsimulations
    allow_failure: true
    script:
    - pwd
    - ls -l
    - python -m mypy qTools/QuantumToolbox/*.py

flake8:
    stage: Static Analysis
    tags:
        - qsimulations
    allow_failure: true
    script:
    - flake8 --max-line-length=140 qTools/*.py

pylint:
    stage: Static Analysis
    tags:
        - qsimulations
    allow_failure: true
    script:
    - pylint -d C0301 qTools/*.py

unit_test:
    stage: Test
    allow_failure: true
    script:
    - pwd
    - ls -l
    - export PYTHONPATH="$PYTHONPATH:."
    - python -c "import sys;print(sys.path)"
    - pytest